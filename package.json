{
  "name": "express-lambda-app",
  "version": "1.0.0",
  "description": "Express app deployed on AWS Lambda",
  "main": "handler.js",
  "scripts": {
    "start": "node local-server.js",
    "dev": "nodemon local-server.js",
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}